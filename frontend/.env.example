# Where the frontend should send /api/* requests.
# - Set to your backend URL in dev/prod.
# - Leave unset (or empty) to use Next.js's /api/* rewrite proxy instead
#   (server-side fetch from Next dev server to the backend; useful when
#   running through a Cloudflare tunnel or similar).
NEXT_PUBLIC_API_URL=http://localhost:8000
